import {createTheme} from '@mui/material/styles';

const Palette = (mode) => {
    return createTheme({
        palette: {
            backgroundColor: '#557744'
        }
    });
};

export default Palette;
